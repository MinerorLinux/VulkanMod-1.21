package net.vulkanmod.vulkan.memory;

public class ConcreteBuffer extends Buffer {
    public ConcreteBuffer(int usage, MemoryType type) {
        super(usage, type);
    }

    // Implement any additional methods or overrides if necessary
}
